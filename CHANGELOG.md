# 更新日志

## 0.3.79

- 重写固定行高虚拟滚动：同帧合并更新、复用缓冲窗口、缓存 DOM，滚动不再无条件触发 doLayout。
- 全量数据和选择集合退出滚动组件的响应式树；render 仅读可见行，选择状态使用 Set 查询。
- 保留 Element UI 样式、列配置、插槽、排序、全选、分页、行定位和实例方法。
- 修复 attach 深度遍历开销，保留顶层替换、数组原地修改及可见行实时更新。
- 补齐监听、尺寸观察、动画帧和样式清理，支持 keep-alive 以及运行时切换虚拟模式。
- 请求代理只回填最新响应；同步抛错、async 拒绝均结束 loading，销毁或更换配置后忽略旧响应。
- 普通表格仅在列配置变化时主动布局，窗口 resize 同帧合并。
- 扁平化列改为单数组累积，修复首轮列 key 不稳定；长段合并改用迭代，避免递归溢栈。
- 新增真实 Vue 模板与 Element UI DOM 回归，覆盖 10 万行滚动和 5 万行连续合并。
- 修正 npm publish 生命周期递归调用，限制发布文件范围，修复 pnpm 下旧版 Vue CLI loader/polyfill 解析。

虚拟模式仍要求固定行高。不新增可变行高、跨窗口合并、树形展开等能力。
